---
$schema: https://holocron.so/frontmatter.json
title: GPUI and React for native GPU-accelerated desktop apps
sidebarTitle: Home
description: GPUIX is React for GPUI, Zed's GPU UI framework. Build native desktop apps with React and TypeScript. No Electron, no web views.
keywords:
  - GPUI
  - GPUI React
  - GPUI TypeScript
  - native GPUI app
prompt: |
  Write the homepage from @/README.md and @/website/src/components/hero-section.tsx.
  Keep the README import as the body. Hero tagline is GPUI and React for native apps.
  Title and heading must include GPUI so the site can rank for that word.
---

import { HeroSection } from './components/hero-section.tsx'
import Readme from '../../README.md'

<Above>

<HeroSection />

</Above>

<Readme />
