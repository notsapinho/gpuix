{
  "name": "gpuix",
  "private": true,
  "license": "Apache-2.0",
  "packageManager": "bun@1.4.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build --filter=@gpuix/react",
    "build:all": "turbo run build",
    "build:native": "cd packages/native && bun run build",
    "build:react": "cd packages/react && bun run build",
    "example:counter": "cd apps/examples && bun --hot counter.tsx",
    "dev": "bun scripts/dev.ts",
    "test": "turbo run test",
    "test:perf": "turbo run test:perf",
    "typecheck": "turbo run typecheck",
    "web": "bun scripts/web.ts",
    "web:wasm": "bun scripts/web.ts --build-only"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "react-refresh": "^0.18.0",
    "tsx": "^4.19.0",
    "turbo": "^2.11.2",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-reconciler": "^0.33.0"
  }
}
